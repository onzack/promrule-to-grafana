# PromRule-To-Grafana (Grafana Managed Alerts Converter)

The "Promrule to Grafana Rule Converter" continuously mirrors PrometheusRule alerting rules from all namespaces into a Grafana instance as Grafana-managed alert rules, using mimirtool against the Grafana /api/convert/ endpoint.
